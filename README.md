# School Management System 🎓

A simplified React-based dashboard to manage school data efficiently.

## 🛠 Tech Stack
- **Frontend:** React, Tailwind CSS, Lucide-React (Icons).
- **Backend:** JSON Server (Mock API).

## 🚀 Getting Started
1. Clone the repo.
2. Install dependencies: `npm install`.
3. Start JSON Server: `npm run server` (or `npx json-server --watch db.json --port 5000`).
4. Run React app: `npm run dev`.

## 📂 Features
- **Role-Based Access Control** (Admin, Teacher, Student).
- Student & Teacher CRUD operations.
- Dynamic dashboard statistics.
- Attendance marking system.
- Course Management.
- Grade Management.
